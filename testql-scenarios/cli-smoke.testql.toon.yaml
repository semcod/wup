# SCENARIO: CLI Smoke Tests
# TYPE: cli
# GENERATED: true

CONFIG[2]{key, value}:
  cli_command, wup
  timeout_ms, 15000


# Test: wup --help
SHELL "wup --help" 5000
ASSERT_EXIT_CODE 0
ASSERT_STDOUT_CONTAINS "usage"

# Test: wup --version
SHELL "wup --version" 5000
ASSERT_EXIT_CODE 0