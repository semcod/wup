project:
  name: "flask-example"
  description: "Flask example project with WUP"

watch:
  paths:
    - "app/**"
  file_types:
    - ".py"
  excludes:
    - "*.md"
    - "tests/**"

services:
  - name: "auth"
    root: "app/auth"
    paths:
      - "app/auth/**"
    type: "auto"
    quick_tests:
      scope: "all"
      max_endpoints: 3

test_strategy:
  quick:
    debounce_s: 2
    max_queue: 5
    timeout_s: 10
